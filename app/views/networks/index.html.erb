<h1 id="network">Your Network</h1>

<% @contacts.each do |contact| %>
  <%= link_to user_path(contact) do %>
  <div class="cards">
    <div>
      <div class="cards">
    <%= cl_image_tag contact.photo.key, height: 300, width: 400, crop: :fill  %>
      </div>
    </div>
   </div>
  <% end %>
<% end %>

